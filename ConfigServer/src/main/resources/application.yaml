server:
  port: 8086
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ajayvaidya37/microservices-config
          clone-on-start: true
